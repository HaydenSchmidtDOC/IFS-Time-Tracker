# ---- Claude Code local files ----
.claude/

# ---- Local MCP server config (personal dev tool, not part of the repo) ----
.mcp.json

# ---- .NET build output ----
bin/
obj/
[Bb]in/
[Oo]bj/
*.user
*.suo
.vs/

# ---- Publish output ----
dist/
publish/
*.publish.xml.user

# ---- Runtime time-log data (personal; syncs via OneDrive, not git) ----
# Build output already covers data/ under bin/ (dotnet run) and dist/ (publish); this
# additionally guards a root-level data/ if the exe is ever run from the repo root.
/data/

# ---- Personal dev notes/bookmarks (not shipped, not project docs) ----
/notes/

# ---- OS noise ----
Thumbs.db
Desktop.ini
